{
  "name": "design-lab",
  "version": "0.1.0-alpha.0",
  "private": true,
  "description": "DESIGN-LAB product Node workspace (single source of truth for the Workbench). Version mirrors the authoritative Python package fact in pyproject.toml (taskpack 19 / FU-07); a release decision must move both, never one alone. The MiniGame fixture keeps its own Node env and is NOT a workspace dependency (taskpack 24).",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "pnpm --filter @design-lab/workbench typecheck",
    "build": "pnpm --filter @design-lab/workbench build",
    "test:workbench-native": "pnpm --filter @design-lab/workbench test:native"
  }
}
