# DESIGN-LAB Python dependencies (reproducible)
# ODA4-0103. Core verifiers use only the Python standard library.
# jsonschema is the only schema-validation dependency (used by evidence-card,
# Domain Pack, and object-model verifiers).
# rpds-py is pinned to modern builds: 0.30.x wheels are broken on Python 3.12
# (missing rpds.rpds binary module), which breaks jsonschema import in CI.
jsonschema>=4.18,<5
rpds-py>=2025.6.0

# Pixel-perfect reconstruction core (installed by canonical CI from this root file).
-r design-lab/reconstruction/requirements-core.in
