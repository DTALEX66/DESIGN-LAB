# Photoshop 复杂参考新任务仍超时

在前次任务通过同步静止确认、正常释放宿主 guard 后，使用当前线性图层读回实现，新建独立任务执行 `prepare_real_poster_ps.py --execute`。不是重复原 attempt，未覆盖旧产物。

新目录：`.project-local/task-artifacts/real-poster-ps-20260908/run-01c37d95fb944e77b40e09b644d4a60e`。99 文本、44 推断透明栅格、89 像素填充；非全矢量或原始隐藏图层恢复。

会话 61929 最终退出 1：PowerShell COM DoJavaScript 调用在 120 秒抛出 TimeoutExpired，经适配器映射 PHOTOSHOP_OUTCOME_UNKNOWN，经任务层映射 NATIVE_OUTCOME_UNKNOWN。未取得原始完成回执，不能认定宿主已停止，也不能宣称读回优化解决整体超时。

后续只调查同一运行目录及宿主阶段，不再直接创建第三次同类制作任务，不单纯扩大超时求绿。须补阶段时间证据，区分制作、保存、重开、读回、PNG 导出。保护锁由本新任务持有，旧任务的释放事实不适用于它。

## 时间线实测

任务 `att-839e089155bf4a6bad45e3677f87ab8b`，数据库事件与 NTFS mtime 统一为 UTC：

- RUNNING：2026-09-08T20:34:09.434388+00:00。
- OUTCOME_UNKNOWN：20:36:09.511670，约 120.077 秒。
- master.psd mtime：20:36:13.871718，距开始约 124.437 秒，晚于超时约 4.360 秒。
- photoshop-preview.png mtime：20:36:20.266914，距开始约 130.833 秒。

固定 psRunJob 顺序为创建与图层制作→保存 PSD 并重开→读回→PNG→再次重开与读回。mtime 不是每阶段开始/结束时间，不能直接计算纯制作或纯保存时长；但足以证明宿主在调用端超时后继续写文件，且制作到首次保存这一组合阶段已跨过 120 秒。只优化最终读回不能保证整体低于当前上限。

下一步应加入固定项目内阶段计时及完成回执持久化，区分调用观察超时与实际宿主终止；在得到新的资格证据前，不将现有两个文件当成功回执，也不通过放宽检查处理。
