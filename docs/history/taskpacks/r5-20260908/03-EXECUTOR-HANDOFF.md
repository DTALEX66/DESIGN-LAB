# 交执行Agent的指令

只处理DTALEX66/DESIGN-LAB，保持独立产品和个人非商业研究定位。先读当前AGENTS.md、.project/paths.json、docs/LOCAL_ENVIRONMENT.md、R3账本和本包。检查远端main与开发分支SHA，若变化先做差量审计；不要覆盖用户已有改动。

先执行DL-R5-001。保留现有R3账本作为唯一活动源，将本包任务/验收/回退/四轴/旧ID增量接入；Markdown由机器源生成。历史文件只作历史记录。任何迁移均记录supersedes、源hash和恢复定位。

已有授权范围内连续完成可逆实现和验证；不逐个低风险动作重复询问。涉及用户人审接受、超出范围的盘符写入、购买/外传或发布时按照实际授权处理。E盘无精确授权不可访问；普通项目任务不能隐式购买或上传。

一任务一个执行写者，变更提交独立可回退。每次结果记录任务ID、base/result SHA、环境、输入输出hash、命令退出码、四轴、局限、下一步、回退。测试替身仅算unit；宿主报告未获取原件标报告可见/原件未核验，不得借旧CI替新提交通过。

先补运行恢复、安装RIR、受控Adobe提交与局部修改。仅批准的job结构可执行，不暴露任意JSX/命令/任意本地路径执行API。外部Agent仅调用同一合同，不各自维护数据库。UIA仅原生接口不足时启用。

合并候选应有当前SHA的CI和关键回归；通过代码验收不等于用户设计接受。没有用户授权不代签Jury或正式release。不为清锁关闭用户共享Adobe/Comfy进程。

M1演示必须由页面触发真实任务：导入→修正对象计划→AI/PSD→两次局部修改→保存关闭重开→对比→人工接受→完整导出。两个格式分别留证；单一截图、整图贴底、手工合成任务事件不得充当整链证据。
