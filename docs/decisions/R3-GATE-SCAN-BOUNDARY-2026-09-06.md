# R3-04 身份门禁扫描边界修复

本地发现：身份门禁对整个工作区 `rglob`，将 `.project-local` 的测试 SQLite
当作 UTF-8 活动源码读取；只在文件枚举后排除部分旧运行目录。
触发输入来自本轮早期测试未关闭连接所留下的两个合成数据库，不是用户数据库。
测试连接生命周期已修正；此前失败结果保留在本项目忽略证据目录。

修复：采用自顶向下遍历，在进入目录前剪除 `.project-local`、旧 `.hermes`、
依赖／缓存和私有 Agent 状态；排除凭据文件。活动 reparse 路径不跟随，报告需复核。
已取消误将全部 `.github` 跳过的前缀判断；仅 `.github/workflows-archive` 的历史
工作流和 `.gitignore` 的排除规则不参与产品身份判定，活动 workflow 仍检查。

负例覆盖：运行 DB 不进入源码判断；用真实子进程 audit hook 验证无忽略目录
`scandir`；合成私有文件不读取；活动违规与不可读源码仍失败；活动／历史 workflow 分离。
这不意味着忽略产物无需检查——产物内容与边界由对应运行态验证器负责。

完整 R3-04 仍未完成：CI 触发覆盖、精确提交远端 CI、Python 3.12 一致环境、
renderer 锁定与干净 checkout 复现尚需执行。本修复没有删除任何已有门禁。
