# Knowledge Staging

This directory is DESIGN-LAB's temporary governed knowledge workspace. It is not a permanent knowledge authority and contains only small, auditable records and compiled projections; original assets remain outside Git.

Lifecycle: `registered → extracted → normalized → candidate → reviewed → compiled`, with `rejected` and `quarantined` terminal branches. Runtime inputs may only reference `reviewed`, `projections`, or explicitly compiled targets.
