{
  "schemaVersion": "design-lab/registry-app-paths/v1",
  "note": "Approved registry / App Paths metadata + per-tool locator inputs (Prompt B-B tier 3). This is the SINGLE tool-locator catalog. It is NOT a second paths SSOT: canonical roots still live in .project/paths.json, and dynamic resolution results (receipts) go to .project-local/. The resolver consults this static approved metadata only; a LIVE Win32 registry query remains an owner-gated extension and is never performed here.",
  "envVarByTool": {
    "open-design": "OPEN_DESIGN_EXE"
  },
  "exeNamesByTool": {
    "open-design": ["Open Design", "Open Design.exe", "opendesign", "opendesign.exe"]
  },
  "registryAppPaths": {
    "open-design": {
      "key": "HKEY_CURRENT_USER\\Software\\Microsoft\\Windows\\CurrentVersion\\App Paths\\Open Design.exe",
      "approval": "owner-registered App Paths entry; resolver reads this metadata only, never the live registry"
    }
  },
  "canonicalRootHintByTool": {
    "comfyui": "design-toolchain/toolchains/comfyui",
    "open-design": "design-toolchain"
  },
  "installAuthorizationRequired": true
}
