# minimax-h3 — E3 取证记录（运行时就绪）

- **任务**：DL-H3-001（H3 适配器）/ DL-H3-002（H3 取证）/ DL-H3-003（H3-Comfy 桥接）
- **状态**：E0 → **E3 运行时就绪**（模型下载完成 + ComfyUI 节点识别验证）
- **取证时间**：2026-08-14（boundTree=`f80d985`）

## 模型文件（ModelScope 国内源，未走代理）

| 文件 | 大小 | 目录 |
|---|---|---|
| `minimax_h3_fl2va_pruned_int8_convrot.safetensors` | 19.53GB | `models/diffusion_models/` |
| `qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors` | 14.61GB | `models/text_encoders/` |
| `minimax_h3_video_vae_fp16.safetensors` | 4.85GB | `models/vae/` |
| `minimax_h3_audio_vae_fp32.safetensors` | 0.56GB | `models/vae/` |

- 来源：`Comfy-Org/MiniMax-H3`（ModelScope 镜像，465GB 全量中的官方推荐组合）
- 下载校验：curl rc=0 + 4 文件全部 `OK`（大小与 ModelScope 一致）
- 许可：`minimax-h3-community-license-agreement`（不 vendoring，仅运行时引用）

## ComfyUI 节点识别证据（读回验证）

- `GET /object_info` → **849 节点**，H3/MiniMax 节点已注册：
  `EmptyMiniMaxH3LatentAV`、`MiniMaxH3ImageToVideo`、`MiniMaxH3ReferenceToVideo`、`MiniMaxH3SigmaShift`、`MinimaxTextToVideoNode` 等
- `UNETLoader` 可选模型：`minimax_h3_fl2va_pruned_int8_convrot.safetensors` ✅
- `CLIPLoader` 可选：`qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors` ✅
- `VAELoader` 可选：`minimax_h3_audio_vae_fp32` + `minimax_h3_video_vae_fp16` ✅

## 硬件与配置（社区共识）

- RTX 5060（8GB VRAM）+ 64GB RAM
- 社区证据：ComfyUI issue #15251 确认 8GB 显存 + partial CPU offload 是支持场景（bug 已修）
- 官方推荐：`int8_convrot`（cu130 下）+ `nvfp4` text encoder（无需 Blackwell）
- ComfyUI 会 CPU offload 到 64GB 内存，推理慢但可用

## 边界

- 首次真实出片（T2V workflow）为 E3 端到端证据——待用户批准运行
- H3-Comfy 桥接（DL-H3-003）：运行时就绪后按 `h3-comfy-bridge-feasibility.md` 执行
- 生成物仅写 `.hermes/task-runtime/` 或 `80-evidence/`
