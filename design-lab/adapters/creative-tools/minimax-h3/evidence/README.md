# minimax-h3 — 取证目录

- **任务**：DL-H3-001/002/003
- **当前状态**：**E0 占位（当前树未执行）**。
- 本轮未启动 MiniMax H3、未启动 ComfyUI、未下载模型、未运行 T2V workflow，也未将任何外部模型/节点/硬件状态认定为当前证据。
- 目录中的旧候选记录仅保留审计痕迹，不绑定当前 exact SHA，不得用于 E3/E4 晋级。

## 当前可证明范围

- 仅证明 adapter manifest、权限/边界声明和目录结构存在。
- H3、ComfyUI、H3-Comfy bridge 和 T2V 出片保持冻结。
- 重新晋级必须在明确授权后完成当前 exact tree 的真实运行、artifact/provenance/read-back 和端到端验证。
