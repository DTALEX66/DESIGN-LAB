# comfyui — E3 取证记录

- **任务**：DL-CFY-001（ComfyUI 适配器）/ DL-CFY-002（ComfyUI 取证）
- **状态**：E0 → **E3**（运行时真实执行 + 读回验证）
- **取证时间**：2026-08-14（boundTree=`0719205`）

## 运行时证据（E3 四要素）

| 要素 | 值 |
|---|---|
| **任务 ID** | DL-CFY-001 / DL-CFY-002 |
| **运行时** | ComfyUI **v0.33.1**（commit `72865f4`）+ Python 3.13.14 + torch 2.13.0+cu130 |
| **位置** | `D:\All projects\Design External Configuration\toolchains\comfyui\ComfyUI_windows_portable\` |
| **启动方式** | `python_embeded/python.exe -s main.py --port 8188 --disable-auto-launch` |
| **GPU** | `cuda:0 NVIDIA GeForce RTX 5060`（VRAM 8150MB，cudaMallocAsync） |
| **健康读回** | `GET /system_stats` → 200，`devices[0].name="cuda:0 NVIDIA GeForce RTX 5060"`，`python_version=3.13.14` |
| **API 验证** | `GET /object_info` → 节点定义可枚举（CheckpointLoaderSimple 等） |

## 完整性证据

- 下载：`ComfyUI_windows_portable_nvidia.7z`（2133107036B，与 GitHub Content-Length 精确一致）
- 校验：7-Zip `test` rc=0（Everything is Ok）
- 解压：56419 文件，4440192768B

## 生成物

- 证据写入 `.hermes/task-runtime/`（忽略目录，不入 Git）
- 运行日志：`.hermes/task-runtime/start_comfyui.py`（启动脚本，guard 包装）

## 边界

- MiniMax H3 模型：下载中（ModelScope 国内源，`models/diffusion_models/` + `text_encoders/` + `vae/`）
- H3 适配器（DL-H3-001/002/003）另取 E3（H3 权重就绪 + 首次出片后）
- ComfyUI 为 GPL-3.0 运行时，模型为 minimax-h3-community-license-agreement——**不 vendoring**，仅运行时引用
