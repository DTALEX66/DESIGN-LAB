# Projections / bundles

Open Design Manifest Projection 生成目标：`packages/capabilities/bundles/` 下的中性能力定义
通过 `verifier/generate_open_design_adapter_indexes.py` 投影为 `open-design.json`。
公共对象不直接以 Open Design Manifest 作为唯一合同（DL-ADP-OD-002）。
